# isOdd

Add an `isOdd` utility function to `src/b/`.

## Requirements

- Function takes a number and returns `true` if odd, `false` if even
- Mirror the existing `isEven` utility in style and location
