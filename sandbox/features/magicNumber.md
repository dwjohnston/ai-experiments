# magicNumber

## Description

A utility function that accepts two numbers, sums them, and returns `true` if the sum is prime.

## Details

- Function name: `magicNumber`
- Input: two numbers
- Output: boolean — `true` if the sum of the two numbers is prime, `false` otherwise

## Location

`src/b/` — implement as a new file following existing conventions in that folder.
